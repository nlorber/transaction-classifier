FROM python:3.11-slim

WORKDIR /app

COPY --from=ghcr.io/astral-sh/uv:latest /uv /usr/local/bin/uv

ENV UV_COMPILE_BYTECODE=1 \
    UV_LINK_MODE=copy

COPY pyproject.toml uv.lock ./
RUN uv sync --frozen --no-dev --extra serve --no-install-project

COPY src/ src/
RUN uv sync --frozen --no-dev --extra serve

# Feature profile YAML is read at runtime via the default TXCLS_FEATURE_PROFILE path.
COPY config/ config/

ENV PATH="/app/.venv/bin:$PATH"

EXPOSE 8000

CMD ["uvicorn", "transaction_classifier.inference.app:get_app", "--factory", "--host", "0.0.0.0", "--port", "8000"]
